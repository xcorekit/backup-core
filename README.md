# sys-core

Workstation system tools: `updatex`, `refreshx`, `serverx`, `creatorx`, `bindx`, `unbindx`.

## Install

```bash
git clone git@github.com:xcorekit/sys-core.git
cd sys-core && ./install.sh
```
